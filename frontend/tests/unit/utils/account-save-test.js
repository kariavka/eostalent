import accountSave from 'frontend/utils/account-save';
import { module, test } from 'qunit';

module('Unit | Utility | account-save', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = accountSave();
    assert.ok(result);
  });
});
